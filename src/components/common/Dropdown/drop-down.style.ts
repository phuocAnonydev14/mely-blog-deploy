import styled from 'styled-components';
import { Dropdown, Menu } from 'antd';

export const DropDownStyle = styled(Dropdown)``;

export const DropDownItemStyle = styled(Menu)`
  ul {
    background-color: var(--color-primary);
  }
`;
